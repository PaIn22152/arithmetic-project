package com.payne.leetCode.p_s;

import java.util.ArrayList;
import java.util.List;

/**
 * Project    arithmetic-project-git
 * Path       com.payne.leetCode
 * Date       2018/08/10 - 12:13
 * Author     Payne.
 * About      类描述：
 */

public class P_31_NextPermutation {
    /**
     * https://leetcode.com/problems/next-permutation/description/
     * Implement next permutation, which rearranges numbers into the lexicographically next greater permutation of numbers.
     * <p_old>
     * If such arrangement is not possible, it must rearrange it as the lowest possible order (ie, sorted in ascending order).
     * <p_old>
     * The replacement must be in-place and use only constant extra memory.
     * <p_old>
     * Here are some examples. Inputs are in the left-hand column and its corresponding outputs are in the right-hand column.
     * <p_old>
     * 1,2,3 → 1,3,2
     * 3,2,1 → 1,2,3
     * 1,1,5 → 1,5,1
     */

    public void nextPermutation(int[] nums) {
       // my(nums);
    }

    public void my(Integer[] nums) {
        List<Integer> oldNum=new ArrayList<Integer>();

        Integer[] objects = oldNum.toArray(nums);

    }
}
